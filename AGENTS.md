# 项目开发规则

本文件约束本仓库的代码、配置和文档修改。项目介绍与使用入口见 [README](README.md)，实现原理见[开发者文档](docs/README.zh-CN.md)。

## 开发方式

- 修改前读取适用的目录约定、现有实现和相关架构，明确修改范围与验收标准；保留其他任务的改动，跨层改动说明原因与影响。
- 当前是 Demo 阶段，没有用户和存量数据。不维护旧接口、旧配置或旧文件格式兼容；错误、废弃及被替代的实现直接删除，同步调用方、测试和文档。
- 完成最小完整实现，不用原型或示例代替正式能力；不保留过渡包装、重复实现和废弃支路。
- 新抽象、配置和依赖须有实际需求；不照搬参考项目整套框架，不因 Web 暂未使用就删除有明确复用价值的接口，不预建热加载或插件市场。
- 目录和包使用职责短名，不加 `agent-` 前缀。Tauri Cargo 包名为 `tauri-plugin-bridge`，目录与工作区别名为 `tauri-bridge`。
- 保留有效的 `Cargo.lock`，依赖变更同步更新；发布构建固定工具链与依赖。
- 有可用 Luna 时，可独立的子任务明确文件所有权后再委派；主代理负责设计、集成和验收。
- 未获明确授权，不提交、推送、部署、改写 Git 历史或重启实际服务。

## 架构约束

- 保持核心层、扩展层、产品层（Web）三层边界。编程、运维、办公只是装配场景，不新增业务架构层；“最小”不意味着削减可靠执行或将扩展降级为样例。
- `api` 只定义通用契约，`runtime` 保留唯一执行循环；核心不依赖 Web、数据库、具体 Provider 或业务场景。修改 Core 前先说明现有扩展点为何不足。
- 扩展先提供可复用普通接口，需要注册或生命周期时再提供薄 Plugin。模型、工具、规则、摘要和存储实现留在扩展层，不锁在 HTTP 路由或页面中。
- Application 通过公共 `AgentRuntime` 调用执行器；Bridge 和 Client 只做传输与展示适配，不导入执行器私有结构或复制执行循环。Planner 只做上层编排。
- Memory 与 Sessions 独立装配；自进化另作策略组件，不成为基础记忆可用的前提，不混入 Runtime 或 Memory 存取逻辑。
- 目录、凭据、权限域、工具集合和部署策略由宿主决定。Web 默认工具不等于所有 Agent 的必选工具；扩展不得扩大宿主授权。
- 主模型与辅助调用共用预算和取消；活动执行状态不得依赖可裁剪的展示缓存。取消不等于回滚，未知副作用不自动重跑，可靠提交必须等待确认。

## Web 与配置约束

- Web 功能必须接通页面、宿主接口、实际能力和保存恢复；未实现或未装配时显示真实不可用状态。
- 可选组件、可选工具和模型配置分开管理；固定能力及部署锁定项不出现在用户开关清单。
- 默认本机流程通过页面配置模型，不强制填写 `.env`；环境变量用于部署覆盖或固定模型装配，基础 Token 和存储密钥由宿主管理。
- 设置保存成功后才发布新状态；活动 Run 固定使用启动时绑定的模型配置。已保存密钥不得回传页面或进入 URL、日志、浏览器持久存储。
- 缺配置、协议错误或调用失败不得切换假模型；测试夹具与产品入口分开。
- UI 遵守 Mona 设计系统及适用的 `docs/ui/DESIGN.zh-CN.md`、`docs/ui/COMPONENTS.zh-CN.md`、`docs/ui/DESIGN-GOVERNANCE.zh-CN.md`。参考项目仅用于已授权范围，不扩大产品目标。

## 包介绍与文档维护

- 根 `README.md` 只介绍项目定位、主要能力、快速开始、结构概览、文档入口和必要使用限制；各包 README 介绍本包职责与用法。开发流程、命名、提交、验证及文档维护规则放在 `AGENTS.md`，不在 README 中重复。
- 公开架构入口为 `docs/README.zh-CN.md`，正文放在 `docs/architecture/`，描述当前实现、接入契约和真实限制，不作为开发规则或方案计划的副本。
- 方案、计划、研究、审计和交付记录不公开，新材料放在公开仓库之外；公开阅读入口不依赖这些资料。已有混放资料与历史清理单独授权，不把目录改名或忽略规则当作历史隔离。
- 包职责、公共行为或协议格式变化时，同批更新调用方、必要版本标识、测试、对应包 README、组件目录及受影响的公开架构；新包必须有 README。
- 功能说明只保留二次开发所需的接口、数据流、配置、权限、错误和生命周期；已有架构章节或包 README 能说明的，不为每个功能另建文档。
- 参数与协议版本按所属源码和文档集中维护，跨文档引用；删除过时说明，不按开发轮次追加旧版本段落。

## 验收与交付

- 按改动风险选择验证，覆盖相关失败、取消、容量与未安装扩展路径；仅修改文档时检查内容、相对链接、锚点和差异，无需为凑数量运行全量测试。
- 开箱流程使用无 `.env`、无既有配置的隔离环境；UI 改动按风险验证真实浏览器。只清理本次测试创建的进程和数据。
- 区分源码检查、编译、受控集成、真实供应商和生产验收；报告实际结果、证据与未完成项，不用历史测试或未运行用例宣称通过。
- 修改结束检查差异，确认未覆盖无关工作；需要提交或发布时另行核对授权及公开范围。
