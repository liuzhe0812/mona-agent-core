# 项目开发约定

## 目标与交付定位

- 交付可用于生产、可嵌入不同应用的组件化最小 Rust Agent Harness。能力通过独立组件提供，由宿主按需装配；Plugin 是接入机制之一。
- “最小”指职责、依赖与实现规模精简；可靠执行必需的校验、权限、取消、预算和错误处理必须完整。
- 通用 Web UI 是正式交付组件，要求开箱启动、通过页面完成用户配置，并支持真实任务执行。不能将它降级为演示页、视觉预览或仅供开发者参考的界面。
- Runtime 可独立嵌入；正式 Web 宿主可默认装配常用组件，但不能因此让 Runtime 强制依赖这些组件的具体实现或 UI。
- 本文件记录已确认的长期约束，不代表所有约束均已实现。旧文档中“Web UI 仅为标准参考界面”的定位已被本约定取代；发现实现或文档不一致时，应明确指出，并在获授权的任务范围内修正。

## 架构边界

| 层 / 目录 | 职责 |
|---|---|
| `packages/api` | 模型、工具、运行、插件与事件的通用契约 |
| `packages/runtime` | 唯一默认执行循环、当前任务状态、模型网关、工具调度、权限与资源控制、插件生命周期 |
| `packages/providers` | 具体模型协议、鉴权、请求与响应转换；通过公共模型接口供执行器调用 |
| `packages/models`、`packages/memory`、`packages/planner` | 可选能力组件；需要运行时注册时可附带薄 Plugin 入口 |
| `packages/application` | 与界面和传输无关的任务调用管理、幂等、订阅、回放与保留策略 |
| `packages/http-bridge`、`packages/tauri-bridge`、`packages/client` | HTTP/Tauri 传输及客户端协议适配；两种桥接可独立选择 |
| `apps/server` | 正式 Web 宿主：装配组件、管理配置与存储、提供管理接口、处理鉴权和部署生命周期 |
| `examples/` | 可运行的接入与组合样例，不自动成为正式产品依赖 |
| `apps/web/` | 正式通用界面、交互与配置流程；通过宿主接口使用已装配能力 |

- 内层只依赖公共契约，不能反向依赖 UI、具体业务或组合根。插件不得导入 Core 私有结构；具体适配器和存储依赖留在实现模块。
- 组件负责能力实现，接口定义边界，宿主负责组合。组件可通过普通 API 直接使用，也可提供薄 Plugin 入口；Plugin 只处理注册与生命周期，不复制能力实现。没有明确需求时，不要求所有组件都插件化。
- 目录与内部包名使用职责短名，不加 `agent-` 前缀。Tauri 桥接目录和工作区别名为 `tauri-bridge`，其 Cargo 包名遵循生态惯例使用 `tauri-plugin-bridge`。JavaScript 客户端包名为 `client`。
- `application` 通过 `AgentRuntime` 调用执行器；通用桥接不直接依赖 Core 或模型适配器。产品宿主可以依赖具体实现进行装配。
- 全项目复用同一套 Agent 执行机制。插件、桥接、UI 和启动器不得各自实现另一套模型决策与工具执行循环。
- 必要能力不等于必须内置具体实现。运行必须有模型实现，但可通过 `HostBuilder.model(...)` 或模型插件提供；多供应商管理和设置页面不应成为最小 Agent 的必需依赖。
- Planner 使用现有执行接口进行上层编排；跨任务 Memory、持久化后端和业务配置留在扩展层。当前任务的执行状态仍由 Core 拥有。
- 管理供应商、凭据和用户设置的接口由可信宿主控制，不能为了页面接入就扩大公共任务请求的权限，也不能默认注册成模型可调用的管理工具。

## 每次任务的工作方式

1. 先读取相关设计与现有实现，确认任务属于哪一层。开工时用简短说明给出修改范围和最终可验收标准。
2. 优先复用现有接口和扩展点，再考虑新增模块。先判断能力归属，不沿调用链习惯性修改所有层。
3. 用户明确的范围是硬约束，例如“只改 UI”。范围内的常规实现直接完成；发现必须跨越范围的缺口时，说明缺口、影响与所需改动，先同步再继续依赖该改动的工作。
4. 修改 Core 或公共协议前，必须说明现有扩展点为何不能满足需求、缺口是否跨业务通用、为何必须由执行器承担，以及兼容性影响。改变已确定的架构、权限或执行语义时，先与用户同步，并更新 ADR 和相关契约。
5. 实现最小完整方案。每个新抽象、依赖、公共服务或配置项都要服务当前需求；尚无实际用途的扩展只作为建议提出。
6. 用户要求原型图时使用图像生成能力；用户要求功能实现时交付可工作的代码与交互，不能用原型替代实现。
7. 长任务链中，将边界清晰、简单且可独立完成的子任务交给 Luna；复杂设计、架构判断、最终集成和验收由主代理负责。明确文件所有权，禁止子代理撤销他人改动。

## 组件装配与配置规则

- 可选性必须真实：不安装扩展时，最小运行组合仍可工作；安装后，能力实际生效。禁止 UI 显示已启用、后端却未装配的假能力。
- 需要注册服务、工具或统一生命周期时，使用已有插件装配机制，并保持 Plugin 入口薄。没有明确需求时，不增加插件市场、动态热加载、万能 Hook 或新的扩展框架。
- 配置与凭据存储先放在拥有该能力的模块内；有实际共用需求后，再提取公共服务。
- 每项用户配置要有明确的持久化来源和覆盖规则。保存失败不能发布新配置，重启不能静默覆盖用户已保存的设置。
- 模型管理页面负责的配置应能在页面完成。默认 Web 启动允许尚未配置模型，先显示可用的设置流程；发起任务前再检查模型是否就绪。
- `.env` 或环境变量可用于部署覆盖、旧配置迁移和未安装管理插件时的固定模型模式，不能成为默认 Web 使用流程中重复填写模型配置的强制步骤。
- Bridge Token、本地存储密钥等基础设施由宿主按部署方式管理；默认本机使用流程不能要求用户手工理解、生成和维护这些内部参数。该要求不取消鉴权与凭据保护。
- 页面可向获授权的宿主提交用户新输入的凭据；已保存密钥不回传浏览器，不进入 URL、日志或浏览器持久存储。
- 模型设置变更默认只影响新任务；已启动任务固定使用启动时解析的模型配置。改变此语义需明确设计和验证。

## Web UI 与完整体验

- 用户可见的正式功能必须完成页面、宿主接口、实际能力和持久化之间的闭环。若本次范围仅允许其中一部分，应明确未接通的部分，不能伪装成功。
- 已实现功能提供可工作的入口、返回路径、加载状态、空状态与失败反馈；未安装插件时明确说明，并保留仍可用的基础功能。
- 正式入口缺少配置或调用失败时，禁止自动切换到假模型或演示数据。测试夹具与离线预览必须明确标记，并与正式使用流程分开。
- 用户指定参考项目时，只参考指定范围。Mona 的 UI 或功能示例、deepseek-harness 的职责划分，不自动构成本项目的新功能清单或框架选型要求。
- 用户明确要求布局一致时，应对照参考做视觉验收；配色、内容与功能范围按用户要求处理。

## 验收与交付

- 测试与改动风险成比例。必要检查通过即停；不为凑数量编写重复实现逻辑的测试，不因为提交而重复运行已通过且未受后续改动影响的检查。
- 设置类功能验证：全新环境启动、首次配置、保存、重启恢复、实际任务使用，以及保存/连接失败时的反馈。必须验证插件未安装的基础路径。
- “开箱启动”必须在没有项目 `.env`、没有既有用户配置的隔离环境中验证，不能依赖开发机残留状态通过验收。
- UI 改动按风险检查真实浏览器交互与布局；关键集成通过实际调用链验证。清楚标注使用本地模拟端点还是真实供应商。
- 区分源码完成、编译通过、测试通过、真实服务联调和生产验收。测试数量不能替代用户流程验收，也不能据此宣称整体生产就绪。
- 交付说明先给结果，再说明必要的使用方式、验证结果和实际未完成项；保持短段落，避免套话。
- 保留用户及其他任务的改动。Git 提交前逐项核对暂存范围，不将无关变更顺手提交；未获授权不提交或推送。
- 文档中的启动命令必须在当前仓库真实存在；更新已改变行为的相关说明，不将历史状态描述当作当前事实。

## 设计入口

- [Packages 组件目录](packages/README.md)
- [Apps 与 Examples 目录](apps/README.md)
- [当前架构](docs/ARCHITECTURE.zh-CN.md)
- [Core 边界与通用性判断](docs/GENERIC-CORE-BOUNDARY.zh-CN.md)
- [插件契约](docs/PLUGIN-GUIDE.zh-CN.md)
- [统一应用接口](docs/APPLICATION-API.zh-CN.md)
- [架构决策记录](docs/ADR.md)
- [模型管理与宿主装配](docs/MODEL-MANAGEMENT.zh-CN.md)

这些文档用于核对具体契约。历史说明与新确认的项目约定冲突时，标出差异，不用过时描述否定已确认的需求；本文件也不能代替对当前实现的检查。
