# 项目开发约定

## 项目目标

- 架构统一为：核心层 + 扩展层 + 产品层（Web）；分别交付最小执行底座、可复用 Agent 能力和 Web 产品。
- 当前是 Demo 开发阶段，没有用户和存量数据。不维护旧接口、旧配置或旧文件格式兼容；错误、废弃及被替代的实现直接删除，同步调用方、测试和文档。
- “最小”限制底座职责和复杂度，不削减可靠执行，也不把扩展能力降级为样例。编程、运维、办公等是装配场景，不是新的架构层。

## 架构边界

| 部分 | 职责与位置 |
|---|---|
| 核心层 | `packages/api` 定义通用契约；`packages/runtime` 维护唯一执行循环、当前运行状态、模型网关、工具调度、预算、取消、权限和必要扩展时点 |
| 扩展层 | `providers`、`tools`、`models`、`compaction`、`spill`、`skills`、`memory`、`planner` 等提供具体能力；会话、存储与规则按实际复用需求组织，不能绑定 Web 才能使用 |
| 产品层（Web） | `apps/server` 装配能力、管理配置与授权；`apps/web` 提供交互与展示；`application` 管理任务调用、幂等与订阅；HTTP/Tauri Bridge 和 `client` 负责传输适配 |
| 示例 | `examples/` 验证接入与组合，不代替正式能力实现 |

- Runtime 可直接嵌入，不依赖 Web、数据库、具体 Provider 或业务场景。Application 通过公共运行接口调用 Runtime，桥接不导入执行器私有结构。
- 能力先有普通可复用接口，需要注册或生命周期时再提供薄 Plugin。所有能力复用同一执行循环；Planner 只做上层编排。
- 某场景必需不等于具体实现必须内置 Core。工具集合、摘要算法、规则文件和存储后端由宿主装配；当前 Web 的默认工具不定义所有 Agent 的最小边界。
- 宿主决定目录、凭据、权限域和部署策略；可复用实现不锁在 HTTP 路由或页面中。同一能力不保留两套行为不同的实现。
- 新抽象、配置和依赖须有明确需求。既不照搬 Pi/DSH 全部机制，也不因当前 Web 未使用就删除有明确扩展用途的接口；不预建热加载、插件市场或万能框架。
- 模型与辅助调用共用预算和取消；执行状态不依赖展示缓存；取消不等于回滚，未知副作用不自动重跑。可靠提交必须等待确认。
- 目录和包使用职责短名，不加 `agent-` 前缀。Tauri Cargo 包名为 `tauri-plugin-bridge`，目录与工作区别名为 `tauri-bridge`。

## 开发方式

- 先读相关约定与实现，简述修改范围和验收标准。遵守用户范围，保留其他任务的改动；必要跨层改动先说明原因与影响。
- 修改 Core 前说明为何现有扩展点不足、为何属于通用执行职责；已确认的架构或执行语义改变同步记录 ADR。
- 直接完成最小完整实现，不用原型替代功能，不保留过渡包装和废弃支路。
- 有可用 Luna 时，边界清楚的独立子任务交给它，明确文件所有权；主代理负责设计、集成与验收。
- 未获授权不提交、推送、部署或重启实际服务。

## 配置与 Web 产品

- Web 是正式交付产品，必须打通页面、宿主接口、实际能力和保存恢复；未实现或未装配的能力不得伪装可用。
- 可选组件、可选工具和模型配置使用独立页面；当前产品固定能力及部署锁定项不出现在开关清单。
- 默认本机流程通过页面配置模型，不强制填写 `.env`。环境变量仅用于部署覆盖或固定模型装配；基础 Token 和存储密钥由宿主管理。
- 设置保存失败不发布新状态；正在运行的任务固定使用启动时绑定的模型配置。密钥不回传页面，不写入 URL、日志或浏览器持久存储。
- 配置缺失或调用失败不切换假模型；提供真实的空状态、错误和返回路径。测试夹具与产品入口分开。
- UI 使用 Mona 设计系统。指定参考项目只参考授权范围，不自行扩展产品目标。

## 包介绍与文档维护

- 包职责或公共行为变化时，同步对应 README、`packages/README.md` 及受影响的产品说明；新包必须有 README。
- 文档描述当前实现，跨包契约与 ADR 通过链接引用；删除过时说明，不复制多份细节、不逐轮堆叠迁移文档。

## 验收与交付

- 围绕真实缺陷和用户流程验证，包含失败、取消、容量与未安装扩展路径；必要检查通过即停，不凑测试数量。
- 开箱流程在无 `.env`、无既有配置的隔离环境验证；涉及 UI 时按风险检查真实浏览器。只清理测试自己创建的进程与数据。
- 分清源码、编译、受控集成、真实供应商和生产验收。交付说明给出结果、证据与未完成项，不用历史测试代替本次验证。

## 设计入口

[组件目录](packages/README.md) · [架构](docs/ARCHITECTURE.zh-CN.md) · [ADR](docs/ADR.md) · [扩展契约](docs/PLUGIN-GUIDE.zh-CN.md) · [应用接口](docs/APPLICATION-API.zh-CN.md) · [能力装配](docs/CAPABILITY-ASSEMBLY.zh-CN.md) · [UI 设计系统](docs/ui/DESIGN.zh-CN.md) · [UI 组件合同](docs/ui/COMPONENTS.zh-CN.md) · [UI 设计治理](docs/ui/DESIGN-GOVERNANCE.zh-CN.md)
