# Mona 开发约定

架构与接口见 [docs/README.zh-CN.md](docs/README.zh-CN.md)；子目录有 `AGENTS.md` 时同时遵守。本文件只保留项目取舍与容易误改的边界。

## 范围与交付

- 当前是 Demo：只保留最新接口、配置和存储格式，删除被替代实现，不写迁移、旧兼容或双轨支路。Demo 不降低已承诺功能的完整性，不用假界面或脚本模型代替正式能力。
- 同一工作区可能有并行任务：只修改当前范围，按当前文件内容精确编辑；不覆盖、回退或混入其他任务的改动。提交、推送、部署、改写历史和重启实际服务须获得对应授权。
- 目录和包采用职责短名，不加 `agent-` 前缀；`tauri-bridge` 的 Cargo 包名固定为 `tauri-plugin-bridge`。依赖和工具链以 `Cargo.lock`、`rust-toolchain.toml` 为准。

## 架构边界

- 三层为**核心层、扩展层、应用层（宿主与界面）**。编程、运维、办公是装配场景，不另建业务层；不因当前 Web 未使用就删除可复用接口。
- `api` 定义通用契约，`runtime` 保留唯一默认执行循环；不依赖 UI、数据库或具体供应商。需要改变 Core 时，先说明现有扩展点为何不足，并取得用户批准。
- 模型、工具、规则、压缩、存储和编排先提供可复用普通接口，需要生命周期时再加薄 Plugin；不锁进 HTTP 路由。Application/Bridge/Client 通过公共执行接口调用，不复制循环。
- Planner 管理同一 Agent，不为计划步骤另建 Run；Subagent 使用独立会话、共享预算与取消，授权只可收紧，父任务结束前须收尾。Memory 与 Sessions 独立装配，自进化不是二者的前提。
- 目录、密钥、工具集合与权限由宿主绑定。Sandbox 对 Agent Shell 和 write/edit 使用同一文件副作用策略，后端失效不能降级扩大权限；不将它宣称为读取、联网或原生插件隔离。计划、审批、沙箱分别负责各自职责。
- 主模型、辅助调用和子任务不能绕过统一预算与取消；展示缓存不是执行正本。取消不等于回滚，未知副作用不自动重跑，持久提交须等待确认。

## Web 与桌面

- 沿用 DSH 的静态 Module/Slot 分工：扩展配套 UI 在本地 catalog 中装配，向 Registry 注册，不在 `app.mjs` 堆业务特判。输入框 `+` 复用 `composer.commands`；不另建动态插件分发或热加载。
- UI 契约不进入 `api::PluginManifest`。注册项须有所有者和释放路径；切换会话/连接后丢弃过期响应。卸载界面不等于取消任务或删除数据，UI Registry 不是安全沙箱。
- 只展示真实接通的能力；区分已编译、期望启用和实际生效。组件、工具、模型分开管理，固定必备能力和部署锁定项不提供用户开关。
- 本机通过页面配置模型，不强制 `.env`；Token 和存储密钥由宿主管理。保存确认后更新状态，活动 Run 不随界面切换模型；密钥不回传页面、不进入 URL、日志或浏览器存储，失败不切换假模型。
- 视觉与交互遵守 [Mona 设计规范](docs/ui/DESIGN.zh-CN.md)、[组件合同](docs/ui/COMPONENTS.zh-CN.md)及[设计门禁](docs/ui/DESIGN-GOVERNANCE.zh-CN.md)；参考项目不自动扩大本次范围。

## 文档与验收

- 根 README 负责定位、已实现能力、启动与使用限制；包 README 负责入口和用法；`docs/architecture/` 负责当前接口、数据流和生命周期。变更时同步调用方、测试、版本定义和相关说明，不重复堆叠方案或按开发批次追加记录。
- 方案、研究、审计和交付记录不公开，新材料放在公开仓库之外；测试证据放在已忽略的目录。已有混放资料和 Git 历史清理另行授权，公开阅读入口不得依赖新增私有记录。
- 验证与风险匹配，覆盖失败、取消、容量和未装配路径；UI 改动验证真实浏览器。开箱测试使用无 `.env`、无既有配置的隔离环境，只清理测试自有进程和数据。
- 复用默认构建目录；二进制被实际服务占用时先申请停服，不用另一套构建链规避。区分源码检查、编译、受控集成、真实供应商与生产验收，未运行和历史结果不算本次通过。
